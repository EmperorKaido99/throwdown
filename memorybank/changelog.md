# Changelog — Ad-hoc Changes

Changes made outside the scope of any task file. Includes quick fixes, opportunistic improvements, or config tweaks noticed during other work.

| Date | Description | Files Changed | Related Task (if any) |
|------|-------------|---------------|-----------------------|
| 2026-07-28 | Agent harness (D5 workflow + memorybank) installed from the public setup prompt. Project name "Shadow Box", ticket prefix `SB`, context in `memorybank/`. | `agents.md`, `.github/copilot-instructions.md`, `.github/skills/`, `memorybank/**`, `README.md` | SB-005 |
| 2026-07-28 | UI shell added around the existing debug harness — main menu, screen routing, mobile-responsive layout, persisted settings. Existing `PunchHarness` reachable unchanged from the Train screen. | `src/App.tsx`, `src/ui/shell/**`, `src/config/settings.ts` | SB-004 |
| 2026-07-28 | Risk log entries 15 (animation-as-trigger + mocap projection) and 16 (player height/reach) recorded. | `docs/05-TECH-SETUP-AND-RISK-LOG.md` | — |
| 2026-07-28 | Phone-run enablement: spoken prompts, screen wake lock, touch abort, stage aspect ratio from the real stream, HTTPS-over-LAN dev scripts. All five are things that would have voided or corrupted a measured run on a phone. | `src/ui/speech.ts`, `src/ui/useWakeLock.ts`, `src/ui/PunchHarness.tsx`, `src/App.tsx`, `src/App.css`, `src/config/settings.ts`, `vite.config.ts`, `package.json` | SB-013 |
| 2026-07-28 | Measurement durability: run conditions (device, screen, camera, delegate, pose FPS at start and end) auto-stamped onto the report with throttling and shoulder-width-fallback warnings; completed runs persisted to localStorage and offered back after a reload; Web Share for getting a report off a phone. | `src/debug/runContext.ts`, `src/debug/savedRun.ts`, `src/ui/PunchHarness.tsx`, `src/App.tsx`, `src/App.css` | SB-001, SB-013 |
| 2026-07-28 | Vercel deployment config — build/output settings and cache headers for the 33 MB MediaPipe WASM and 5.6 MB model. Verified the WASM glue makes no `SharedArrayBuffer` reference, so no COOP/COEP cross-origin-isolation headers are required. | `vercel.json` | SB-013 |
| 2026-07-29 | Run 2 failed at 0% detection. Root-caused to an FSM bug — the punch episode could finalise before the fist had left the guard radius, so it measured the wind-up and rejected on excursion while the fist went on to travel 12x the gate. Fixed with a `clearedGuard` latch; two regression tests added that fail without it. Risk log 17. | `src/perception/punchClassifier.ts`, `src/perception/punchClassifier.test.ts`, `src/debug/runContext.ts`, `src/ui/DetectionDiagnostics.tsx` | SB-001 |
| 2026-07-29 | Guided protocol reworked into spoken coaching: 8s lead-in to walk back into frame, a spoken block intro per punch type with a form cue and an explicit SWITCH HANDS call, and a per-rep cue that names the hand ("cross, right hand") instead of just the punch. Ready window 1400 to 1800 ms; the 2000 ms capture window is unchanged. | `src/perception/punchScript.ts`, `src/ui/PunchHarness.tsx`, `src/App.css`, `src/perception/punchClassifier.test.ts` | SB-001 |
| 2026-07-29 | A recovered run from an older build was re-shared and read as a fresh result; identifying the build took inspecting the JSON for a field that had since been added. Runs now carry a build stamp (Vercel commit SHA, or a local timestamp), the results screen shows a RECOVERED RUN banner, and the recovery offer names the build it came from. | `vite.config.ts`, `src/vite-env.d.ts`, `src/debug/runContext.ts`, `src/ui/PunchHarness.tsx`, `src/App.css` | SB-001 |
| 2026-07-29 | Run 3 (build 8525a1b, 80 trials, 1 detected): guard-clearance fix confirmed working, but the jitter-scaled gate — unbounded since risk log 14 fix 5 — had inflated the excursion threshold from 0.13 to 1.08/1.28 because the player stood far enough back to halve torso scale. Both derived radii now clamped, and calibration quality is flagged on screen and in the report. Risk log 19. | `src/config/tuning.ts`, `src/perception/punchClassifier.ts`, `src/perception/calibration.ts`, `src/ui/PunchHarness.tsx`, `src/debug/runContext.ts`, `src/App.css` | SB-001 |
| 2026-07-29 | Run 4 (build d54443a, 80 trials, 0 detected, 8 timeouts): calibration was sampling while the player was still settling, producing 0.31 torso units of "guard jitter" — larger than the re-arm radius, so no episode could ever close. Three fixes: calibration ignores moving frames, detection measures travel from a rolling resting baseline and also closes on retraction, and timeouts are recorded. The run button is now disabled on a warned calibration. Risk log 20. | `src/config/tuning.ts`, `src/perception/calibration.ts`, `src/perception/punchClassifier.ts`, `src/ui/PunchHarness.tsx` | SB-001 |
| 2026-07-29 | Milestone 3 started: `src/simulation/fightSim.ts` — deterministic, side-effect-free `(state, input) → state` fight simulation with health, wind-up, recovery, stun, slip/duck defence resolved at the resolving tick, KO and time-up outcomes. 15 tests including determinism, non-mutation and replay-from-mid-fight. Punch-set agnostic: works with a single punch type if Milestone 1 descopes. | `src/simulation/fightSim.ts`, `src/simulation/fightSim.test.ts`, `src/config/tuning.ts` | SB-006 |
